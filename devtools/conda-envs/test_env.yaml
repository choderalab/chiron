name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - jax
  - openmm
  - tqdm
  - loguru
  - openmm
  - openmmtools
  - h5py
  - jaxopt

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  # - pip:
  #  - codecov
