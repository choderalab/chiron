name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - jax
  - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
