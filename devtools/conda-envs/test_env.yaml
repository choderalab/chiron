name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - jax
  - openmm
  - tqdm
  - loguru
  - openmm
  - openmmtools

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
     - h5py
  #  - codecov
